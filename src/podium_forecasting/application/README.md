# Application

Own use-case coordination through domain objects and ports. Depend only on the standard library, domain, ports and this layer. Keep provider clients and deployment configuration outside this layer. No extraction, training, evaluation or publication workflow is implemented yet.

The executable import policy in `quality-policy.toml` is authoritative. This directory establishes responsibility and contains no placeholder Python implementation.
