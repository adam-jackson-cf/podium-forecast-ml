# Domain

Own racing and forecasting concepts, validated value objects and pure calculations once their product contracts are agreed. Use the standard library and this layer only. Do not read files, environment variables or external services. No forecasting rules are implemented yet.

The executable import policy in `quality-policy.toml` is authoritative. This directory establishes responsibility and contains no placeholder Python implementation.
