# Entrypoints

Own process startup, explicit configuration and assembly of application workflows with concrete adapters. This layer may compose the other layers. Keep calculations and external-service implementations in their owning layers. No forecasting job or publication command is implemented yet.

The executable import policy in `quality-policy.toml` is authoritative. This directory establishes responsibility and contains no placeholder Python implementation.
