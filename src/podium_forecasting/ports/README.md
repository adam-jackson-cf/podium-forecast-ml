# Ports

Own typed contracts for capabilities required by application workflows, such as dataset storage or publication. Depend only on the standard library, domain and this layer. Introduce contracts alongside an actual use case; no speculative adapters or forecasting interfaces are implemented yet.

The executable import policy in `quality-policy.toml` is authoritative. This directory establishes responsibility and contains no placeholder Python implementation.
